
server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/library_jbdl?createDatabaseIfNotExist=true
    username: root
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

users:
  student:
    authority: std
  admin:
    authority: adm

book:
  return:
    due-date: 14

logging:
  level:
    org:
      springframework: info